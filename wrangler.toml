name = "job-scout"
compatibility_date = "2026-03-01"
pages_build_output_dir = "dist"

# Interactive search is fully BYOK — no server-side Firecrawl secret.
#
# Daily email digests are deferred (UI removed). KV binding below is unused
# until digests return (e.g. webhook-based). Safe to leave or remove.
#
# [[kv_namespaces]]
# binding = "DIGESTS"
# id = "bc3242b50d0148d6b376144580f18926"
# preview_id = "7c3c22c5d5fc481daa3cb0d0710d03bd"
